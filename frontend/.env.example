# Base URL of the backend API. Never hard-code this in source; it is
# read at build/runtime via import.meta.env.VITE_API_URL so it can be
# changed per environment (local, staging, Kubernetes namespace, etc).
VITE_API_URL=http://localhost:3000
